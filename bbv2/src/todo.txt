fix issue with music not updating when browser is refreshed 
(refresh should go back to start menu and reset everything)

convert names to use a map so I can use them in non-react components
add an indicator whenver a certain hp bar is damaged


do something like isscreendark but permanent for phase 3

Do stat buffs/debuff reversion gradually. Like it checks if anything is abnormal every turn, and it bumps it up or down accordingly

For status effects, they’re there for a set amount of turns unless an item is used.
need to remove dead people from item targets

add glow borders to status effects